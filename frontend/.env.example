# REQUIRED: base URL for frontend -> backend calls (must be set for Activity feed and other API calls).
# Must be reachable from the browser (host).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Auth mode: local by default for self-hosted deployment.
# The frontend stores a session token in browser sessionStorage and sends Bearer auth to backend APIs.
NEXT_PUBLIC_AUTH_MODE=local
