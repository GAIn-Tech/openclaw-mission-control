name: Build and Push Container Image

# Container is now built and pushed by cloud-claw's wrangler deploy.
# This workflow is disabled (manual-only) for reference.
on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "MC container is built by cloud-claw repo via mc-container/Dockerfile"
